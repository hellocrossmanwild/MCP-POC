# Database connection (required)
DATABASE_URL=postgresql://user:password@localhost:5432/contractor_search

# Google OAuth 2.0 (required for production auth)
GOOGLE_CLIENT_ID=your-google-oauth-client-id

# API key for testing without Google OAuth (optional)
MCP_API_KEY=your-api-key-here

# Server port (optional, defaults to 5000)
PORT=5000
